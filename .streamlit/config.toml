[server]
headless = true
enableCORS = false
enableXsrfProtection = false
# 연결 끊긴 후 세션 유지 시간 (초) — 기본 120초 → 1시간
disconnectedSessionTTL = 3600
# WebSocket 핑 간격 (초) — 연결 유지용
websocketPingInterval = 30

[browser]
gatherUsageStats = false
